import styled from 'styled-components';

const Page = styled.div`
  display: flex;
  flex-direction: column;
  height: 100vh;
  section {
    flex: 1;
    display: flex;
    main {
      flex: 1;
    }
  }
`;

export default Page;
